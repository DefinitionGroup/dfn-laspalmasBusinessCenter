# Add these after we create the dedicated Sanity project together.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2026-07-14

# Public site origin used by metadata, previews, and revalidation.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only values added after the project exists.
SANITY_API_READ_TOKEN=
SANITY_API_WRITE_TOKEN=
SANITY_REVALIDATE_SECRET=
SANITY_STUDIO_PREVIEW_ORIGIN=http://localhost:3000
